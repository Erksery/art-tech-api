import { STATUS_VALUES } from './constants.config';

export const StatusConfig = {
  all: [STATUS_VALUES.APPROVED, STATUS_VALUES.PENDING],
  approved: [STATUS_VALUES.APPROVED],
  pending: [STATUS_VALUES.PENDING],
};
